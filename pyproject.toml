[project]
name = "micro-mind"
version = "0.1.0"
description = "Evolutionary task execution and learning system"
requires-python = ">=3.11"

[tool.setuptools.packages.find]
where = ["."]
include = ["micro_mind*"]

[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"
